# Frontend

Next.js 15 (App Router) + TypeScript + Tailwind. Phase 0 placeholder only — design system and chat UI are Phase 7.

```bash
cp .env.example .env.local
pnpm install
pnpm dev
```

Open http://localhost:3000
